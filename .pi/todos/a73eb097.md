{
  "id": "a73eb097",
  "title": "[Medium] AI レスポンスからのテキスト抽出コードが 3 箇所に重複",
  "tags": [
    "refactor",
    "pi-git"
  ],
  "status": "open",
  "created_at": "2026-06-06T03:45:44.579Z"
}

## 場所
- `src/core/diff-analyzer.ts:268`
- `src/core/auto-commit-message.ts:215`
- `src/core/auto-commit-message.ts:424`

## 問題
以下のコードが 3 箇所に重複している：
```typescript
result.content
  .filter((c): c is { type: "text"; text: string } => c.type === "text")
  .map((c) => c.text)
  .join("")
```

## 修正案
共有ユーティリティ関数に抽出する。既存の `extractTextContent` (`auto-commit-message.ts:265`) を拡張して `ContentBlock[]` 入力にも対応させるか、新しいヘルパー関数を作成する。
