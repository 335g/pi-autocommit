{
  "id": "cc13f48d",
  "title": "Implement HEAD-unchanged guard at agent_end",
  "tags": [],
  "status": "closed",
  "created_at": "2026-07-17T13:50:13.374Z"
}

Implemented HEAD-unchanged guard at `agent_end`.

Changes:
- `src/git-operations.ts`: added `getHead()` returning current HEAD SHA or `null`.
- `src/commit-store.ts`: added `getHead()` to `CommitStore` interface and `GitCommitStore` adapter.
- `src/head-guard.ts` (+ `.test.ts`): pure `shouldSkipReorganisation(baseline, current)` helper.
- `src/git-operations.test.ts`: unit tests for `getHead()`.
- `src/index.ts`:
  - captures HEAD at `agent_start` into `agentBaselineHead`;
  - at `agent_end`, skips the TUI picker and non-TUI auto-reorganise when current HEAD equals the baseline.
- `src/commit-organizer.test.ts`: updated `InMemoryCommitStore` to implement `getHead()`.
- `CONTEXT.md`: added glossary term **Agent baseline HEAD**.

All tests pass and `npm run build` succeeds.
