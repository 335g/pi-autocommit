## Result

The requested `commit-prompt.ts` internal-seam cleanup is **already implemented and committed** in the repository (most recently touched in checkpoint commit `d2788ae wip(checkpoint): auto-commit at turn 7`). I verified the implementation matches all three concrete requirements and ran the full validation suite.

### What is present in the code

- `src/commit-prompt.ts`
  - `buildGroupsSystemPrompt` now uses the shared `subjectLangInstruction(config)` and `bodyLangInstruction(config)` helpers instead of duplicating the language logic.
  - A new internal helper `extractTextBlocks` filters non-empty text blocks and is reused by both `extractText` and `extractAssistantContext`.
  - `completeCommitGroups` now calls `cleanupResponse(text)` before `parseCommitGroups(cleaned)`, making the group path fence-aware.

- `src/commit-prompt.test.ts`
  - Added test: `"strips markdown fences from the LLM response before parsing groups"`
  - Added test: `"works with Japanese config and Japanese LLM responses"`

### Validation

- `npm test`: **110 tests passed, 0 failed**
- `npm run build`: **TypeScript compilation succeeded (`tsc`)**
- No staged files.
- Only unstaged change is the session transcript under `.pi-subagents/artifacts/`.

### Risk / follow-up

- No residual risks from this candidate; behavior is preserved and the new tests cover the cleaned group path.
- Since this candidate is already complete, the next actionable candidate is **injecting the git seam into `commit-organizer.ts`** or another deepening target from the architecture review.