はい、worker2 セッションは完了しています。

## worker2 の結果

候補 **「commit-organizer に git seam を注入」** を実装しました。

### 変更内容

- **`src/commit-store.ts`**（新規）
  - `CommitStore` interface を定義
  - 本番用アダプター `GitCommitStore` を実装（`GitOperations` の薄いラッパー）

- **`src/commit-organizer.ts`**
  - `organizeWipCommits` が `GitOperations` を内部で生成しないように変更
  - 新しいシグネチャ：`(ctx, config, event, store, complete?)`
  - `store: CommitStore` を介してすべての git 操作を行うように整理
  - `ExtensionAPI` の import を削除

- **`src/index.ts`**
  - `agent_end` ハンドラで `GitCommitStore` を生成し、`organizeWipCommits` に渡すように更新

- **`src/commit-organizer.test.ts`**
  - `InMemoryCommitStore`（テスト用アダプター）を追加
  - `organizeWipCommits` のテストを 6 件追加：
    - git リポジトリ外では no-op
    - WIP コミットが 0 なら no-op
    - 単一 WIP → 1 つの論理グループに再編成
    - 複数 WIP → 複数グループに再編成
    - グループ提案失敗時の単一コミット fallback
    - 各グループを独立に stage して index をクリーンに保つ

### 検証結果

- `npm test`: **116 tests passed**
- `npm run build`: **成功**

変更は checkpoint コミットとして自動コミットされています。次はどうしますか？